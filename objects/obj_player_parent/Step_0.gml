///@desc Update our timer variables

if (cooldown > 0) { cooldown--; } //ensure we can fire our weapon again soon

direction = image_angle; // Ensure the player is always facing the correct way as they move